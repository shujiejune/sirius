singlePackage: true

# Dependencies whose postinstall scripts pnpm is allowed to run.
# esbuild installs its platform binary; sharp installs native libvips bindings.
allowBuilds:
  esbuild: true
  sharp: true

minimumReleaseAgeExclude:
  - astro@7.2.7
